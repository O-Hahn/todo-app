apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: todo-oh-route
  namespace: todo-oh
spec:
  path: /
  to:
    kind: Service
    name: todo-oh-service
  port:
    targetPort: 8080-tcp
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect